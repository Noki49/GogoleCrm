{% extends 'GogoleMainBundle:Main:index.html.twig' %}

{% block body %}
    <h2 class="text-center"><span>M</span>odifier une catégorie de contact</h2>
    <div class="row">
    	<div class="col-md-offset-5">
			{{ form_start(edit_form) }}
				<p><b><span>T</span>ype de contact </b></p>
				{{ form_widget(edit_form.typeContact) }}
				<input type="submit" class="btn btn-default" value="Modifier" />
			
				<a href=" {{ path('categoriecontact') }}">
					<input type="button" class="btn btn-default" value="Retour" />
				</a>
				
			{{ form_end(edit_form) }}
			{{ form(delete_form) }}
		</div>
	</div>
{% endblock %}
