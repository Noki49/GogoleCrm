{% extends 'GogoleMainBundle:Main:index.html.twig' %}

{% block body %}
    <h2 class="text-center">Créer une nouvelle catégorie de contact</h2>
    </div class="row">
    	<div class="col-md-offset-5">
			{{ form_start(form) }}
				<p>{{ form_row(form.typeContact) }}</p>
				<input type="submit" class="btn btn-default" value="Créer" />
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
